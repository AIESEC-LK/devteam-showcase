version: '3.8'

services:
  devteam-showcase:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: devteam-showcase
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
